"use strict";

var test;
(function (test) {
    test.testing = true;
    function check() {
        if (!test.testing) throw 'Method is not available when test mode is turned off.';
    }
    test.check = check;
})(test || (test = {}));
module.exports = test;